# SQLite 开发环境配置（不依赖Redis）
DB_DRIVER=sqlite
DB_PATH=./data/app.db

# 服务器配置
SERVER_PORT=8080
GIN_MODE=debug

# JWT配置
JWT_SECRET=dev_jwt_secret_key_here_must_be_at_least_32_chars_long
JWT_EXPIRE_HOURS=24

# Redis配置（开发环境可以禁用）
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=
# REDIS_DB=0
